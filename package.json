{
  "name": "ipsp-js-sdk",
  "version": "1.0.8",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "npm install && bower install && bower update && gulp",
    "prepublishOnly":"npm install && bower install && bower update && gulp",
    "start": "gulp watcher"
  },
  "files": ["dist","src"],
  "unpkg": "dist/checkout.min.js",
  "devDependencies": {
    "bower": "^1.8.0",
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-html-to-js": "0.0.5",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^3.0.0",
    "gulp-watch": "^4.3.11",
    "gulp-wrap": "^0.13.0"
  }
}
